let clouds = [];
let cloudSum = 1;

let stackingPosition = 0;
let scaleCtrl = 1

let destX = 0;
let destY = 0;
let amt = 1;

let xAY;
let yAY;
let viewedAY;

let xAC;
let yAC;
let viewedAC;

let xAB;
let yAB;
let viewedAB;

let xAH;
let yAH;
let posAH
let viewedAH;

let xVE;
let yVE;
let viewedVE;

let xVJ;
let yVJ;
let viewedVJ;

let xVL;
let yVL;
let viewedVL;

let xVM;
let yVM;
let viewedVM;

let xVS;
let yVS;
let viewedVS;

let xVX;
let yVX;
let viewedVX;

function loadVariables(){
  //audioYangpu left
  xAY = random(100*scaleCtrl,width/2-200*scaleCtrl)+random(-10,10)
  yAY = random(height*0.7,height*0.8)+random(-10,10)
  viewedAY = false

  //audioBaoshan right
  xAB = random(width/2+200*scaleCtrl,width-100*scaleCtrl)+random(-10,10)
  yAB = random(height*0.7,height*0.9)+random(-10,10)
  viewedAB = false

  //audioHongkou left
  xAH = random(100*scaleCtrl,width/2-200*scaleCtrl)+random(-10,10)
  yAH = random(height*0.7,height*0.9)+random(-10,10)
  viewedAH = false

  //audioChongming right
  xAC = random(width/2+200*scaleCtrl,width-100*scaleCtrl)+random(-10,10)
  yAC = random(height*0.7,height*0.9)+random(-10,10)
  viewedAC = false

  //videoEastNanjingRd left
  xVE = random(100*scaleCtrl,width/2-200*scaleCtrl)+random(-10,10)
  yVE = random(height*0.7,height*0.9)+random(-10,10)
  viewedVE = false

  //videoJinganTemple right
  xVJ = random(width/2+200*scaleCtrl,width-100*scaleCtrl)
  yVJ = random(height*0.7,height*0.9)
  viewedVJ = false

  //videoLujiazui left
  xVL = random(100*scaleCtrl,width/2-200*scaleCtrl)
  yVL = random(height*0.7,height*0.9)
  viewedVL = false

  //videoMiddleHuaihaiRd right
  xVM = random(width/2+200*scaleCtrl,width-100*scaleCtrl)
  yVM = random(height*0.7,height*0.9)
  viewedVM = false

  //videoSuzhouCreek left
  xVS = random(100*scaleCtrl,width/2-200*scaleCtrl)
  yVS = random(height*0.7,height*0.9)
  viewedVS = false

  //videoXujiahui right
  xVX = random(width/2+200*scaleCtrl,width-100*scaleCtrl)
  yVX = random(height*0.7,height*0.9)
  viewedVX = false
}

function setup() {
  let canvas = createCanvas(windowWidth, windowHeight);
  canvas.parent("p5-canvas-container");
  loadVariables()
  console.log("current canvas size:",width,"*",height)

  scaleCtrl = width/2000
  console.log("current scale:",scaleCtrl)
}

function draw() {
  background(220);
  console.log("last stacking position",stackingPosition)

  drawBackground();
  
  //drawing clouds
  for(let i = 0; i < cloudSum; i++){
    clouds.push(new cloud())
  }
  for(let i = 0; i < clouds.length; i++){
    clouds[i].display()
    clouds[i].update()
  }
  for(let i = clouds.length-1; i>=0; i--){
    //check if it's on canvas
    if(clouds[i].onCanvas == false){
      //if not on canvas, delete!
      clouds.splice(i,1)
    }
  }
  // console.log(clouds.length)
  
  //drawing debris (Audio)
  audioYangpu()
  audioBaoshan()
  audioHongkou()
  audioChongming()
  
  //drawing debris (Video)
  videoLujiazui()
  videoMiddleHuaihaiRd()
  videoSuzhouCreek()
  videoXujiahui()
  videoEastNanjingRd()
  videoJinganTemple()
  
}

function drawBackground(){
  //sky part (smog)
  for(let i=0;i<height*0.8;i++){
    bgtr=205-i*0.5
    bgy=i*1
    strokeWeight(0.7)
    stroke(100,bgtr)
    fill(100,bgtr)
    line(0,bgy,width,bgy)
  }

  //ground part (desert)
  for(let i=0;i<height*0.2;i++){
    bgtr=255-i*15
    bgy=height*0.7-i*2
    strokeWeight(2)
    stroke(224,160,92,bgtr)
    fill(224,160,92,bgtr)
    line(0,bgy,width,bgy)
  }
  fill(224,160,92)
  stroke(224,160,92)
  rect(0,height*0.7,width,height*0.3)
}

class cloud{
  constructor(){
    this.x = random(width*0.6,width*1.1);
    this.y = random(0,height*0.2)
    this.speed = random(0.1,0.5)
    this.fill = random(150,200)
    this.transparency = random(220,255)
    this.onCanvas = true;
  }
  update(){
    this.x -= this.speed
    if(this.x<-50){
      this.onCanvas = false
    }
  }
  display(){
    push()
      scale(2)
      translate(this.x, this.y)
      noStroke();
      fill(this.fill,this.transparency);
      rect(0, 0, 60, 20, 100);
      circle(18, 0, 20);
      circle(35, 0, 30);
    pop()
  }
}

function audioYangpu(){
  push()
  translate(xAY,yAY)
  rotate(-0.144)
  scale(scaleCtrl*1.5)
    //draw reference block
    // noFill()
    // stroke(0)
    // strokeWeight(1)
    // rect(-30,-70,60,140)

    noStroke()
    fill(230)
    rect(-15,50-90,30,110)
    beginShape()
    vertex(55-70,50-90)
    vertex(55-70,30-90)
    vertex(63-70,44-90)
    vertex(67-70,41-90)
    vertex(72-70,48-90)
    vertex(75-70,39-90)
    vertex(85-70,50-90)
    endShape()
    
    fill(205,62,47)
    rect(55-70,60-70,30,10)
    fill(205,55,47)
    rect(55-70,80-70,30,10)
    fill(195,62,47)
    rect(55-70,100-70,30,10)
    pop()

  //detect mouse click
  if(mouseIsPressed == true){
    // console.log("mouseClick works!!")

    if(mouseX>xAY-50*scaleCtrl && mouseX<xAY+50*scaleCtrl && mouseY<yAY+50*scaleCtrl && mouseY>yAY-50*scaleCtrl){
      window.open("./assets/Audio/Yangpu/Yangpu.html", "newwindow", "width=1280 height=720 top=0, left=0, toolbar=no, menubar=no, scrollbars=no, resizable=no,location=no, status=no")

      if(viewedAY == false){
        // console.log("viewing successful")
        stackingPosition++
        viewedAY = !viewedAY
      }
    }
  }
}

function audioBaoshan(){
  push()
  translate(xAB,yAB)
  rotate(0.233)
  scale(scaleCtrl)
    //draw reference block
    // noFill()
    // stroke(0)
    // strokeWeight(1)
    // rect(-70,-70,140,140)

    stroke(0)
    strokeWeight(3)
    line(0,-50,-60,-55)
    line(0,-50,30,-60)
    line(30,-60,40,-55)
    
    noStroke()
    fill(139,87,66)
    rect(-70,-50,140,100)
    fill(173,216,230)
    rect(-60,-40,90,80)
    
    fill(200)
    circle(50,-30,20)
    circle(50,-5,20)
    
    fill(0)
    rect(35,10,30,5,5)
    rect(35,20,30,5,5)
    rect(35,30,30,5,5)
    pop()

  //detect mouse click
  if(mouseIsPressed == true){
    // console.log("mouseClick works!!")

    if(mouseX>xAB-50*scaleCtrl && mouseX<xAB+50*scaleCtrl && mouseY<yAB+50*scaleCtrl && mouseY>yAB-50*scaleCtrl){
      window.open("./assets/Audio/Baoshan/Baoshan.html", "newwindow", "width=1280 height=720 top=0, left=0, toolbar=no, menubar=no, scrollbars=no, resizable=no,location=no, status=no")

      if(viewedAB == false){
        // console.log("viewing successful")
        stackingPosition++
        viewedAB = !viewedAB
      }
    }
  }
}

function audioHongkou(){
  push()
  translate(xAH,yAH)
  rotate(0.867)
  scale(scaleCtrl)
    //draw reference block
    // noFill()
    // stroke(0)
    // strokeWeight(1)
    // rect(-70,-70,140,140)

    stroke(180)
    strokeWeight(3)
    line(-30,-30,-30,70)
    line(30,-30,30,70)
    line(-30,-30,30,-30)
    line(-30,0,30,0)
    line(-30,30,30,30)
    
    fill(205,79,57)
    stroke(205,79,57)
    textAlign(CENTER,CENTER)
    textSize(100)
    textStyle(BOLD)
    text("虹",0,0)
    pop()

  //detect mouse click
  if(mouseIsPressed == true){
    // console.log("mouseClick works!!")

    if(mouseX>xAH-50*scaleCtrl && mouseX<xAH+50*scaleCtrl && mouseY<yAH+50*scaleCtrl && mouseY>yAH-50*scaleCtrl){
      window.open("./assets/Audio/Hongkou/Hongkou.html", "newwindow", "width=1280 height=720 top=0, left=0, toolbar=no, menubar=no, scrollbars=no, resizable=no,location=no, status=no")

      if(viewedAH == false){
        // console.log("viewing successful")
        stackingPosition++
        viewedAH = !viewedAH
      }
    }
  }
}

function audioChongming(){
  push()
  translate(xAC,yAC)
  rotate(2.33)
  scale(scaleCtrl)
    //draw reference block
    // noFill()
    // stroke(0)
    // strokeWeight(1)
    // rect(-70,-70,140,140)

    noFill()
    stroke(255,127,36)
    strokeWeight(30)
    circle(0,0,110)
    
    stroke(255)
    fill(255)
    strokeWeight(1)
    rect(-10,-70,20,31)
    rect(-10,39,20,31)
    rect(39,-10,31,20)
    rect(-70,-10,31,20)
  pop()

  //detect mouse click
  if(mouseIsPressed == true){
    // console.log("mouseClick works!!")

    if(mouseX>xAC-50*scaleCtrl && mouseX<xAC+50*scaleCtrl && mouseY<yAC+50*scaleCtrl && mouseY>yAC-50*scaleCtrl){
      window.open("./assets/Audio/Chongming/Chongming.html", "newwindow", "width=1280 height=720 top=0, left=0, toolbar=no, menubar=no, scrollbars=no, resizable=no,location=no, status=no")

      if(viewedAC == false){
        // console.log("viewing successful")
        stackingPosition++
        viewedAC = !viewedAC
      }
    }
  }
}

function videoEastNanjingRd(){
  push()
  translate(xVE,yVE)
  scale(scaleCtrl)
    //draw reference block
    noFill()
    stroke(0)
    strokeWeight(1)
    rect(-70,-70,140,140)

    
  pop()

  //detect mouse click
  if(mouseIsPressed == true){
    // console.log("mouseClick works!!")

    if(mouseX>xVE-50*scaleCtrl && mouseX<xVE+50*scaleCtrl && mouseY<yVE+50*scaleCtrl && mouseY>yVE-50*scaleCtrl){
      window.open("./assets/Video/EastNanjingRd/EastNanjingRd.html", "newwindow", "width=1280 height=720 top=0, left=0, toolbar=no, menubar=no, scrollbars=no, resizable=no,location=no, status=no")

      if(viewedVE == false){
        // console.log("viewing successful")
        stackingPosition++
        viewedVE = !viewedVE
      }
    }
  }
}

function videoJinganTemple(){
  push()
  translate(xVJ,yVJ)
  scale(scaleCtrl)
    //draw reference block
    noFill()
    stroke(0)
    strokeWeight(1)
    rect(-70,-70,140,140)

    
  pop()

  //detect mouse click
  if(mouseIsPressed == true){
    // console.log("mouseClick works!!")

    if(mouseX>xVJ-50*scaleCtrl && mouseX<xVJ+50*scaleCtrl && mouseY<yVJ+50*scaleCtrl && mouseY>yVJ-50*scaleCtrl){
      window.open("./assets/Video/JinganTemple/JinganTemple.html", "newwindow", "width=1280 height=720 top=0, left=0, toolbar=no, menubar=no, scrollbars=no, resizable=no,location=no, status=no")

      if(viewedVJ == false){
        // console.log("viewing successful")
        stackingPosition++
        viewedVJ = !viewedVJ
      }
    }
  }
}

function videoLujiazui(){
  push()
  translate(xVL,yVL)
  rotate(0.33)
  scale(scaleCtrl*1.8)
    //draw reference block
    noFill()
    stroke(0)
    strokeWeight(1)
    rect(-20,-70,40,140)

    noStroke()
    fill(185)
    rect(-5,10,10,40)
    fill(185)
    triangle(-4,10,4,10,0,-70)
    rect(-4,-20,8,40)
    
    strokeWeight(3)
    stroke(185)
    line(-5,-20,5,-20)
    line(-5,10,5,10)
    line(-5,30,5,30)
    
  
    
    noStroke()
    fill(222,21,90)
    // arc(0,55,30,30,-PI*5/4,PI/4,OPEN)
    // circle(0,-15,25)
    circle(0,55,30)
    
    strokeWeight(3)
    stroke(139,26,26)
    line(-14,55,14,55)
  pop()

  //detect mouse click
  if(mouseIsPressed == true){
    // console.log("mouseClick works!!")

    if(mouseX>xVL-20*scaleCtrl && mouseX<xVL+20*scaleCtrl && mouseY<yVL+50*scaleCtrl && mouseY>yVL-50*scaleCtrl){
      window.open("./assets/Video/Lujiazui/Lujiazui.html", "newwindow", "width=1280 height=720 top=0, left=0, toolbar=no, menubar=no, scrollbars=no, resizable=no,location=no, status=no")

      if(viewedVL == false){
        // console.log("viewing successful")
        stackingPosition++
        viewedVL = !viewedVL
        
      }
    }
  }
}

function videoMiddleHuaihaiRd(){
  push()
  translate(xVM,yVM)
  scale(scaleCtrl)
    //draw reference block
    noFill()
    stroke(0)
    strokeWeight(1)
    rect(-70,-70,140,140)

    
  pop()

  //detect mouse click
  if(mouseIsPressed == true){
    // console.log("mouseClick works!!")

    if(mouseX>xVM-50*scaleCtrl && mouseX<xVM+50*scaleCtrl && mouseY<yVM+70*scaleCtrl && mouseY>yVM-70*scaleCtrl){
      window.open("./assets/Video/MiddleHuaihaiRd/MiddleHuaihaiRd.html", "newwindow", "width=1280 height=720 top=0, left=0, toolbar=no, menubar=no, scrollbars=no, resizable=no,location=no, status=no")

      if(viewedVM == false){
        // console.log("viewing successful")
        stackingPosition++
        viewedVM = !viewedVM
        
      }

    }
  }
}

function videoSuzhouCreek(){
  push()
  translate(xVS,yVS)
  scale(scaleCtrl)
    //draw reference block
    noFill()
    stroke(0)
    strokeWeight(1)
    rect(-70,-70,140,140)

    
  pop()

  //detect mouse click
  if(mouseIsPressed == true){
    // console.log("mouseClick works!!")

    if(mouseX>xVS-50*scaleCtrl && mouseX<xVS+50*scaleCtrl && mouseY<yVS+70*scaleCtrl && mouseY>yVS-70*scaleCtrl){
      window.open("./assets/Video/SuzhouCreek/SuzhouCreek.html", "newwindow", "width=1280 height=720 top=0, left=0, toolbar=no, menubar=no, scrollbars=no, resizable=no,location=no, status=no")

      if(viewedVS == false){
        // console.log("viewing successful")
        stackingPosition++
        viewedVS = !viewedVS
      }
    }
  }
}

function videoXujiahui(){
  push()
  translate(xVX,yVX)
  rotate(-1.113)
  scale(scaleCtrl*1.5)
    //draw reference block
    // noFill()
    // stroke(0)
    // strokeWeight(1)
    // rect(-70,-25,140,50)

    noStroke()
    fill(0)
    beginShape()
    vertex(0-70,45-70)
    vertex(140-70,45-70)
    vertex(138-70,48-70)
    vertex(140-70,50-70)
    vertex(132-70,57-70)
    vertex(135-70,63-70)
    vertex(133-70,68-70)
    vertex(138-70,72-70)
    vertex(131-70,77-70)
    vertex(140-70,83-70)
    vertex(136-70,90-70)
    vertex(0-70,90-70)
    vertex(9-70,82-70)
    vertex(3-70,75-70)
    vertex(6-70,66-70)
    vertex(2-70,57-70)
    vertex(0-70,51-70)
    vertex(6-70,45-70)
    endShape()
    
    textSize(20);
    textAlign(CENTER, CENTER);
    
    fill(255)
    textSize(20);
    textAlign(CENTER, CENTER);
    text("徐家汇站",70-70,62-70)
    
    textSize(9);
    textAlign(CENTER, CENTER);
    text("Xujiahui Station",70-70,80-70)
  pop()

  //detect mouse click
  if(mouseIsPressed == true){
    // console.log("mouseClick works!!")

    if(mouseX>xVX-50*scaleCtrl && mouseX<xVX+50*scaleCtrl && mouseY<yVX+25*scaleCtrl && mouseY>yVX-25*scaleCtrl){
      window.open("./assets/Video/Xujiahui/Xujiahui.html", "newwindow", "width=1280 height=720 top=0, left=0, toolbar=no, menubar=no, scrollbars=no, resizable=no,location=no, status=no")

      if(viewedVX == false){
        // console.log("viewing successful")
        stackingPosition++
        viewedVX = !viewedVX
      }
    }
  }
}